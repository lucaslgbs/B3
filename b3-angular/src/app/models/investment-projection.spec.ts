import { InvestmentProjection } from './investment-projection';

describe('InvestmentProjection', () => {
  it('should create an instance', () => {
    expect(new InvestmentProjection()).toBeTruthy();
  });
});
